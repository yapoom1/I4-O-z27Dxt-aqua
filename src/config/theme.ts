/**
 * THEME MODE CONFIGURATION
 * ─────────────────────────────────────────────
 * Change THEME_MODE to control the app-wide color scheme.
 *
 *  'auto'  → Follows the OS / browser system preference (default).
 *             Light on light-mode systems, dark on dark-mode systems.
 *
 *  'light' → Always light theme, regardless of system preference.
 *
 *  'dark'  → Always dark theme, regardless of system preference.
 * ─────────────────────────────────────────────
 */

export const THEME_MODE: "auto" | "light" | "dark" = "light";
